#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#include <fstream>
using namespace std;

int arr[1001];
int dy[1001];

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	/*fstream cin;
	cin.open("input.txt");*/
	
	return 0;
}
